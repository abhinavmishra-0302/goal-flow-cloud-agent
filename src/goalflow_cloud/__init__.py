"""GoalFlow cloud agent.

Owns conversation + memory, decomposes user goals into Task Contracts
(Contract v0, see /CONTRACT.md), and acts as the WebSocket hub between
the UI and the on-device agent.
"""

__version__ = "0.1.0"
