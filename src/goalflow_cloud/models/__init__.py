"""Pydantic models for GoalFlow CONTRACT v2 (canonical spec: /CONTRACT.md).

Generic, domain-agnostic protocol: ``domain`` names the use case; scope and
context are free-form objects; every model is lenient on unknown fields.
"""
